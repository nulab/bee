# @repo/cli-utils

Internal package. Shared CLI utilities used by `@nulab/bee` commands.

- Output formatting (`outputArgs` / `outputResult` for `--json` support)
- Table rendering (`printTable`)
- Argument parsing (`splitArg` for comma-separated values with valibot validation)
