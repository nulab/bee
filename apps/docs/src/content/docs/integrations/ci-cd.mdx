---
title: CI/CD での利用
description: GitHub Actions などの CI/CD パイプラインから bee を使って Backlog の課題やプルリクエストを自動操作する方法
---

import { Steps, Tabs, TabItem, LinkCard, CardGrid } from "@astrojs/starlight/components";

bee は CI/CD 環境でも利用できます。自動テストやデプロイのパイプラインから Backlog の課題やプルリクエストを操作できます。

## セットアップ

<Steps>

1. **API キー認証を設定する**

   CI/CD では API キー認証を使います。CI/CD サービスのシークレット機能を使って、環境変数に API キーとスペース情報を設定してください。

   ```sh
   export BACKLOG_API_KEY=your-api-key
   export BACKLOG_SPACE=your-space.backlog.com
   ```

2. **パイプラインから bee を呼び出す**

   `npx` を使えばインストール不要で bee を実行できます。

   ```sh
   npx @nulab/bee issue list --project MY_PROJECT
   ```

</Steps>

:::caution
API キーをコードにハードコードせず、CI/CD サービスのシークレット機能を使って管理してください。
:::


## GitHub Actions での例

### 基本: デプロイ後に課題にコメントする

デプロイ完了後に、関連する Backlog 課題へ自動でコメントを追加する例です。

```yaml
name: Notify Backlog
on:
  push:
    branches: [main]

permissions: {}

env:
  BACKLOG_API_KEY: ${{ secrets.BACKLOG_API_KEY }}
  BACKLOG_SPACE: ${{ secrets.BACKLOG_SPACE }}

jobs:
  notify:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Comment on issue
        run: npx @nulab/bee issue comment PROJECT-123 --body "main ブランチにデプロイしました ($COMMIT_SHA)"
        env:
          COMMIT_SHA: ${{ github.sha }}
```

## レシピ

実践的なワークフローの例を用意しています。

<LinkCard title="PR と課題を連動させる" href="../../recipes/pr-lifecycle-sync" description="PR の作成・編集・マージに連動して課題のステータス更新やクローズを自動化" />
<LinkCard title="リリース時に課題へ通知" href="../../recipes/release-notify-issues" description="リリースタグの作成時に関連課題へコメントし、リリースノートを Backlog ドキュメントに投稿" />
